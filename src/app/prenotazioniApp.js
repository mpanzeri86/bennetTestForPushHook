import * as React from 'react';
import * as ReactDOM from 'react-dom';
import PrenotazioniAppComponent from './components/PrenotazioniAppComponent';

ReactDOM.render(
    <PrenotazioniAppComponent />
    , document.getElementById('root'));
